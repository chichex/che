version: 2

builds:
  - main: .
    binary: che
    ldflags:
      - -s -w -X github.com/chichex/che/cmd.Version={{.Version}}
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64

archives:
  - formats: [tar.gz]

homebrew_casks:
  - repository:
      owner: chichex
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Casks
    homepage: https://github.com/chichex/che
    description: "che - workflow estandarizado para trabajar con agentes de IA sobre issues de GitHub"
    license: MIT
    skip: "{{if .Prerelease}}true{{end}}"
    binaries:
      - che
    hooks:
      pre:
        install: |
          system "/bin/bash", "-c", 'brew list --cask che-beta 2>/dev/null && (echo "che: desinstalando cask beta para evitar conflicto de binario..." && brew uninstall --cask che-beta) || true'
      post:
        install: |
          system "xattr", "-dr", "com.apple.quarantine", "#{staged_path}/che"
          system "codesign", "--sign", "-", "--force", "#{staged_path}/che"

  - name: che-beta
    repository:
      owner: chichex
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Casks
    homepage: https://github.com/chichex/che
    description: "che-beta - versión pre-release de che para testing"
    license: MIT
    skip: "{{if not .Prerelease}}true{{end}}"
    binaries:
      - che
    hooks:
      pre:
        install: |
          system "/bin/bash", "-c", 'brew list --cask che 2>/dev/null && (echo "che-beta: desinstalando cask estable para evitar conflicto de binario..." && brew uninstall --cask che) || true'
      post:
        install: |
          system "xattr", "-dr", "com.apple.quarantine", "#{staged_path}/che"
          system "codesign", "--sign", "-", "--force", "#{staged_path}/che"

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
